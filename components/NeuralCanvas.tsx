"use client";

export default function NeuralCanvas() {
  // Removed old particle canvas — mesh background is handled via CSS orbs
  // This component is now a no-op placeholder to avoid import errors
  return null;
}
